{
  "name": "recruitment-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "config:cache": "npm.cmd config set cache \"../.npm-cache\" --location=project",
    "dev": "npx.cmd vite --host",
    "start": "npx.cmd vite --host",
    "build": "npx.cmd vite build",
    "preview": "npx.cmd vite preview --host"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
